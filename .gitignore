# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
.python-version

# Django
*.sqlite3
*.sqlite3-journal
/staticfiles/
media/

# Tailwind
# output.css IS committed: PythonAnywhere has no build step, so the compiled
# CSS ships via git. Rebuild locally (bin/tailwindcss -i input.css -o output.css
# --minify) and commit when templates or input.css change. The Fly Docker build
# regenerates it regardless.
node_modules/

# Tooling
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/

# OS
.DS_Store
Thumbs.db

# Editor
.idea/
.vscode/
*.swp

bin/tailwindcss*

# Claude Code / superpowers session state — local tooling, not project artifacts
.claude/
.superpowers/
